# data/

Place your input video(s) here (e.g. `traffic.mp4`, `footpath.mp4`).

Videos are excluded from Git via `.gitignore` to keep the repository
small — only this placeholder file and `.gitkeep` are tracked.

Any short (30 sec - 2 min) clip of a road, footpath, or entrance with
visible people/vehicles works well for testing.
